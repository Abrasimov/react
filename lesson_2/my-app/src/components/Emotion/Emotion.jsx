import {Button, Header} from "./Emotion.styled";

const Emotion = () => {
    return <Button color={'green'}>
        <Header color={'yellow'}>Header component</Header>
        <p>Button component</p>
    </Button>
}

export default Emotion;
